# Development datasource configuration
# Uses environment variables when provided, with local-safe defaults.
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/kaamconnect}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:MYSQLPASSWORD}

# Optional JDBC tuning for local development
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
